Your goal is to update any vulnerable dependencies.

Do the following:

1. Run `npm audit` to identify vulnerable dependencies in your project.
2. Run `npm audit fix` to automatically update any dependencies that can be safely updated.
3. Run tests and verify the updates do not break your application.